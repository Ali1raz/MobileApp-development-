import 'package:flutter/material.dart';

class LearnTables extends StatelessWidget {
  const LearnTables({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Tables')));
  }
}
